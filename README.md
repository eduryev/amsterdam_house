Shared board state for https://eduryev.github.io/amsterdam_house/

`board.json` is written by the board in each of our browsers, every few seconds.
It is on its own branch so that saving a card does not trigger a Pages rebuild
of `main`. Nothing else belongs here, and the commit noise is expected.
